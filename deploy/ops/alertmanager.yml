route:
  receiver: operator-placeholder
  group_by: [alertname]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

# Local/standalone fallback. Pulumi renders the production receiver at this
# path from encrypted stack configuration without exposing the webhook URL.
receivers:
  - name: operator-placeholder
