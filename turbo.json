{
  "$schema": "https://turborepo.dev/schema.json",
  "globalDependencies": [
    "bun.lock",
    "package.json",
    "bunfig.toml",
    "Cargo.toml"
  ],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", "!dist/.gitkeep"],
      "outputs": [
        "dist/**",
        "build/**",
        ".svelte-kit/**",
        "pkg/**"
      ]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "lint": {
      "dependsOn": ["^lint"],
      "outputs": []
    },
    "test": {
      "dependsOn": ["^test"],
      "outputs": []
    },
    "fast": {
      "outputs": []
    },
    "format": {
      "outputs": []
    }
  }
}
